Vendored third-party code (MIT), checked in verbatim with its licence:
- `splatjs/` — training kernel (WP3.1)
- `splat-transform/` — `.sog` encoder core (WP2.6)
Nothing here is fetched at runtime; `client/` must stay servable as static files.
